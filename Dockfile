
FROM node:8.11.2-alpine

# Create directory
RUN mkdir /joe
WORKDIR /joe

# Install app dependencies
COPY ./dist/* /joe
RUN npm config set registry "http://registry.cnpmjs.org"
RUN npm install -g forever && npm install

EXPOSE 8080
CMD  forever /node/joe/main.js
